let nameIs = "Billy";

let corectName = (nameIs=="Billy") ? "Hello there Billy" : "And who might you be?";

alert(corectName);

//IF the name is Billy, first message is sent. Else, second one.
//This is short hand for if-else.